/*
 * package com.rag.chat.repository;
 * 
 * import org.springframework.data.domain.Page; import
 * org.springframework.data.domain.Pageable; import
 * org.springframework.data.jpa.repository.JpaRepository; import java.util.UUID;
 * 
 * import com.rag.chat.entity.ChatMessage;
 * 
 * public interface ChatMessageRepository extends JpaRepository<ChatMessage,
 * UUID> { Page<ChatMessage> findBySessionId(UUID sessionId, Pageable pageable);
 * }
 */